# from django.db import connection
#
# def my_custom_sql(self):
#     with connection.cursor() as cursor:
#         cursor.execute("INSERT INTO user_preferences (user_id,genre_one_id,genre_two_id,genre_three_id)")
#         row = cursor.fetchone()
#
#     return row